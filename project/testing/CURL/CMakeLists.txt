cmake_minimum_required(VERSION 3.12)
project(consume_curl C)
list(INSERT CMAKE_MODULE_PATH 0 ${PROJECT_SOURCE_DIR}/../../..)
find_package(CURL REQUIRED)
add_executable(consume_curl consume_curl.c)
target_link_libraries(consume_curl PRIVATE CURL::libcurl)
